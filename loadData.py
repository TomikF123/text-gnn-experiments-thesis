from dataset import TextDataset
import loaders
from prepData import clean_data
from utils import get_data_path

def create_file_name(dataset_config)-> str:

    return
def load_data(dataset_config) -> TextDataset:
    if create_file_name(dataset_config) not in get_data_path():
        pass
    
    pass